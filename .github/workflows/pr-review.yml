name: PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: eddiegah/PRSentinel@master
        with:
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
