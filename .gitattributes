# Normalize line endings to LF in the repo regardless of the checkout
# platform, so Windows checkouts stop showing "LF will be replaced by
# CRLF" warnings on every git operation.
* text=auto eol=lf

# Actual binary files - never touch line endings on these.
*.bin binary
*.sqlite3 binary
*.pickle binary
*.ico binary
*.png binary
